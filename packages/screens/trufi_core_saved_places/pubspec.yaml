name: trufi_core_saved_places
description: "Saved places management for Trufi apps - manage favorite locations, history, and custom places"
version: 1.0.0
homepage: https://github.com/trufi-association/trufi-core
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  equatable: ^2.0.5
  flutter_bloc: ^8.1.3
  provider: ^6.1.5
  shared_preferences: ^2.5.3
  trufi_core_interfaces: ^1.0.0
  trufi_core_maps: ^1.0.0
  trufi_core_utils: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
  generate: true
