name: trufi_core_notifications_example
description: Example app for trufi_core_notifications package.
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  trufi_core_notifications:
    path: ../

dependency_overrides:
  trufi_core_interfaces:
    path: ../../trufi_core_interfaces
  trufi_core_utils:
    path: ../../trufi_core_utils

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
