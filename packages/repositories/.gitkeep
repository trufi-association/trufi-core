This is not part of a real layer it is more to enable the BloC pattern to work with other features.

Repositories responsibility is to connect the Data Layer and the Presentation Layer. When the UI wants to fetch data it would use the repositories. 