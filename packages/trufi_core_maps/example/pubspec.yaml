name: trufi_core_maps_example
description: Example app demonstrating trufi_core_maps features.
publish_to: none

environment:
  sdk: ^3.10.0
  flutter: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  trufi_core_maps:
    path: ../
  latlong2: ^0.9.1
  provider: ^6.1.2

dependency_overrides:
  trufi_core_interfaces:
    path: ../../trufi_core_interfaces

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
