# Trufi Core Base Widgets
These are the base widgets of Trufi Core and should be independent from external dependencies as much as possible.
This would help us if we want to have a component library like [widget book](https://www.widgetbook.io/) in the future.
